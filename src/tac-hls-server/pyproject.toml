[project]
name = "tac-sdk-server"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "openai>=2.0.0",
    "fastapi",
    "uvicorn[standard]",
    "python-dotenv",
    "requests",
    "httpx",
    "twilio",
    "python-multipart",
    "pydantic>=2.0.0",
    "structlog",
]
