[project]
name = "owl-health-gemini-agent"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "google-cloud-aiplatform>=1.60.0",
    "google-adk>=0.1.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
